server {
    listen   80;
    server_name localhost;

    location / {       
        index index.php index.html index.htm;
        auth_basic "Enter password";.
        auth_basic_user_file /var/www/pma/htpasswd;
    }
}
